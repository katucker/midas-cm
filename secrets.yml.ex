---
# Example variables file for the sensitive parameters.
# Rename this to secrets.yml and edit to make it unique for your instance.
# The git repository is set to ignore the secrets.yml file, so it won't
# inadvertently copied into the repo and shared with others.
midas_database_password: midas

# Amazon Web Services credentials
#aws_access_key:
#aws_secret_key:

# Private key to use for DomainKeys Identified Mail.
# Default is Javascript code to dynamically read the private key at runtime.
#midas_dkim_private_key: fs.readFileSync("private_key.pem")

